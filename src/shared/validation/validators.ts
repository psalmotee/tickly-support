/**
 * Shared Validators
 * Custom Zod validators and refinements
 */

// Custom validators will be collected here
// Example: export const validateEmail = (email: string) => { ... }
// Example: export const validateTicketId = (id: string) => { ... }
