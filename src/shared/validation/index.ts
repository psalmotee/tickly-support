/**
 * Shared Validation Schemas and Rules
 * Zod schemas and validation functions used across features
 */

// Validation schemas are exported on-demand as they are created
// Schemas should be domain-agnostic and reusable
