/**
 * Domain Types
 * Core business domain types used across features
 */

// Domain types for User, Organization, Ticket, etc.
// Example: export type User = { id: string; email: string; ... }
// Example: export type Organization = { id: string; name: string; ... }
// Example: export type Ticket = { id: string; title: string; ... }
