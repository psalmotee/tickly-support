/**
 * Shared Services
 * Base classes, interfaces, and utilities for services
 */

// Shared service patterns and utilities
// Example: export abstract class BaseService { ... }
// Example: export interface IService { ... }
