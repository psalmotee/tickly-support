/**
 * Shared Repositories
 * Interfaces and base classes for repository pattern
 */

// Base repository interfaces
// Example: export interface IBaseRepository<T> { ... }
// Example: export interface IUserRepository extends IBaseRepository<User> { ... }
