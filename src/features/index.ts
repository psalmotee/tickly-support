/**
 * Features Barrel Export
 * Central export point for all feature modules
 */

// Feature modules are exported on-demand in their respective index.ts files
// This prevents circular dependencies and allows for tree-shaking
